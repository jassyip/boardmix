services:
  boardmix:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: boardmix
    restart: unless-stopped
    ports:
      - "${BOARDMIX_PORT:-8088}:80"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/healthz"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
